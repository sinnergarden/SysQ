# SysQ Configuration
# Copy this file to settings.yaml and fill in your values

# 1. Data Sources
tushare_token: "YOUR_TUSHARE_TOKEN"

# 2. Notifications
webhook_url: "https://example.com/webhook"  # Enterprise WeChat / DingTalk / Slack Webhook

# 3. Paths (Relative to Project Root)
data_root: "data"

# 4. Trading Settings
account_name: "sim_account_001"
fee_rate: 0.0005 # 0.05%
tax_rate: 0.001  # 0.1% (Sell only)
