# Production Model Manifest
# This file defines the approved production model for daily ops
# 
# Usage:
# - Read by ModelScheduler.resolve_production_model() to determine which model to use
# - Update this file when promoting a new candidate to production
# - Can rollback by changing 'model_path' back to a previous version
#
# Format:
#   model_path: relative or absolute path to the production model directory
#   version: identifier for this manifest version
#   promoted_at: ISO timestamp of when this was set
#   status: "active" or "rollback"
#   note: human-readable note about this production model

model_path: "data/models/qlib_lgbm"
version: "2025-03-22-v1"
promoted_at: "2025-03-22T00:00:00"
status: "active"
note: "Initial production model - Alpha158 baseline features"