# Requests — 需求/功能请求追踪

> 本目录存放尚未纳入正式 use case 的需求/功能请求。
> 每个请求一个文件，格式自由（markdown）。
> 需求稳定后升级到 `usecases/` 并注册到 `01_usecase_index.md`。

## 规则

1. 新需求先在 `requests/` 写提案。
2. 经过讨论确认 → 升级为 use case 文档。
3. 被拒绝或搁置 → 保留在 `requests/` 作为记录。
4. 不要直接修改 `usecases/` 下的文档来匹配未确认的需求。
