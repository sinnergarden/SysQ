[Unit]
Description=Qsys Candidate Post-Close — batch dispatch all candidate strategies
Documentation=docs/ops/DAILY_OPS_SOP.md
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
# User=your_user
WorkingDirectory=/path/to/SysQ
# EnvironmentFile=-/path/to/SysQ/.env
# Environment=HTTPS_PROXY=http://proxy:port
# Environment=HTTP_PROXY=http://proxy:port
ExecStart=/path/to/python scripts/run_daily_batch.py \
    --stage candidate \
    --mode postclose \
    --trade-date auto \
    --output-root runs
StandardOutput=append:/var/log/qsys/candidate-postclose.log
StandardError=append:/var/log/qsys/candidate-postclose.log

[Install]
WantedBy=default.target
