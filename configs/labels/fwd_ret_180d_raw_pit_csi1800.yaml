label_id: fwd_ret_180d_raw_pit_csi1800
formula:
  type: forward_return
  horizon: 180
  price: close
  price_basis: adjusted_close
  adjustment_factor: factor
universe: csi1800_pit_union
pit_universe_artifact: csi1800_pit_v2
require_clean_provenance: true
# Strict PIT CSI1800 raw forward-return label, namespaced so it cannot clobber
# the CSI800 store.  Training rows are filtered by each row's dated PIT
# membership; current inference membership is supplied separately and never
# read from this label store.  The Top10 UC advances end_date to the decision
# date, while LabelStore's 181-session maturity gate keeps immature tails out.
date_range:
  start_date: "2018-01-01"
  end_date: "2026-08-10"
