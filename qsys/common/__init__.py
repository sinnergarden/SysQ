"""Generic business-neutral utilities.

See individual submodules:

- ``io`` — JSON file read/write, directory archiving
- ``time`` — ISO timestamps, filename-safe timestamps
- ``git`` — commit hash lookup
"""
from __future__ import annotations
